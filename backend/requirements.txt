# FLOATCHAT backend — Part 0 scaffold.
# Pinned loosely (>=) for now; will be tightened once ingestion/science code lands.

# Web framework
fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
websockets>=13.1

# Database
sqlalchemy>=2.0
asyncpg>=0.30
geoalchemy2>=0.15
alembic>=1.14

# Scientific stack (Part 5+)
xarray>=2024.9
numpy>=2.1
scipy>=1.14
pandas>=2.2
netCDF4>=1.7

# ML/clustering — DBSCAN for Part 8's basic (non-ML classification) event grouping. No
# XGBoost/RandomForest: event *classification* is deliberately rule-based, not ML, per the
# MVP scope lock in PLAN.md.
scikit-learn>=1.5

# Caching (optional, Part 4+)
redis>=5.1

# HTTP client — used by app/ingestion/argovis_client.py (runtime) and tests (below)
httpx>=0.27

# Dev / test
pytest>=8.3
pytest-asyncio>=0.24
